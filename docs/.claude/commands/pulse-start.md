Start the Pulse production server for this project.

First ensure a build exists (`public/dist/`). Then run:

```bash
pulse start
```

The production server uses the port from `pulse.config.js` (defaults to 3000).
